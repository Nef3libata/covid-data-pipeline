[project]
name = "covid-pipeline"
version = "1.0.0"
dependencies = [
    "pandas>=2.0",
    "pyarrow",
]

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]
